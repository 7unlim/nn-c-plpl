#ifndef DATA_AGGREGATION
#define DATA_AGGREGATION

#include <string>
#include <iostream>
#include <fstream>

    void transform_data();
    void encode_data(const std::string& filePath);
    // std::vector<std::string> split(const std::string &str, char delimiter);

#endif